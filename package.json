{
  "name": "summerhacks2026-root",
  "version": "1.0.0",
  "description": "Root package manager for hacking",
  "scripts": {
    "dev": "npm run dev --prefix pacpay",
    "build": "npm run build --prefix pacpay",
    "start": "npm run start --prefix pacpay",
    "install-all": "npm install --prefix pacpay"
  }
}
